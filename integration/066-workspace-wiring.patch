diff --git a/apps/ide/app.js b/apps/ide/app.js
index 33aef10c5948e45c853ebe27486f92d126c55451..6d30513cf6b1b4eb17292010e53431da70dbd478 100644
--- a/apps/ide/app.js
+++ b/apps/ide/app.js
@@ -1 +1,2 @@
+import {WorkspaceJournal,sourceTransaction,snapshotFiles} from './packages/workspace/journal.js';
 import {createStudio} from './packages/workbench/studio.js';
@@ -11,2 +12,5 @@ let workbench=null,studio=null;const recentlyClosed=[],collapsedFolders=new Set(
 const storageKey='jailbreak.workspace.v1';
+const journal=new WorkspaceJournal();
+const historyFile=tx=>tx?.changes.find(c=>Object.hasOwn(files,c.path))?.path??Object.keys(files)[0]??'';
+const sharedDesignHistory={apply(map,t){journal.commit(map,sourceTransaction([{path:t.file,before:t.before,after:t.after}],t.label??'Designer edit'));return t;},undo(map){const t=journal.apply(map,'undo');return t?{...t,file:historyFile(t)}:null;},redo(map){const t=journal.apply(map,'redo');return t?{...t,file:historyFile(t)}:null;}};
 $('file-input').accept+=',.dll,.exe,.nupkg';
@@ -16,5 +20,5 @@ const profiles=createBuildProfileInspector({onApply:()=>{lastBuild=null;save();c
 $('run').parentElement.insertBefore(profiles.button,$('run'));
-const development=createDevelopmentWorkbench({getFiles:()=>files,getActive:()=>generated?'jailbreak-app.js':active,
+const development=createDevelopmentWorkbench({sharedHistory:sharedDesignHistory,getFiles:()=>files,getActive:()=>generated?'jailbreak-app.js':active,
   openFile:(path,offset=0)=>{openFile(path);editor.focus();editor.setSelectionRange(offset,offset);editor.scrollTop=Math.max(0,(editor.value.slice(0,offset).split('\n').length-4)*23);syncScroll();},
-  applySource:path=>{lastBuild=null;profiles.invalidate();modified=true;openFile(path);save();},
+  applySource:path=>refreshWorkspace(path),
   build:restart=>compile(restart===true),persist:save,
@@ -78,3 +82,3 @@ function renderFiles(){
 function renderOptions(project='',entry=''){for(const [id,paths,first]of [['project',Object.keys(files).filter(p=>p.endsWith('.csproj')),'Auto-detect project'],['entry',Object.keys(files).filter(p=>/\.a?xaml$/i.test(p)),'Auto-detect']]){$(id).replaceChildren();const o=document.createElement('option');o.value='';o.textContent=first;$(id).append(o);for(const path of paths){const item=document.createElement('option');item.value=path;item.textContent=path;$(id).append(item);}}$('project').value=project;$('entry').value=entry;}
-function setWorkspace(workspace,{restore=false}={}){if(!workspace.files||typeof workspace.files!=='object')throw new Error('Workspace must contain a files map');const entries=Object.entries(workspace.files);if(entries.length>2000||entries.some(([p,t])=>typeof t!=='string'||t.length>4_000_000))throw new Error('Workspace exceeds safe interactive source limits');if(entries.reduce((n,[p,t])=>n+t.length,0)>16_000_000)throw new Error('Workspace exceeds 16 MB');workbench?.reset();recentlyClosed.length=0;collapsedFolders.clear();profiles.set(workspace.profile??{});development.set(workspace.development??{});files={...workspace.files};symbolSources={};name=workspace.title??workspace.name??workspace.id??'Local workspace';active=workspace.active&&files[workspace.active]!==undefined?workspace.active:entries.find(([p])=>/\.a?xaml$/i.test(p))?.[0]??entries[0]?.[0]??'';opened=workspace.opened??[active,...entries.filter(([p])=>p.endsWith('.cs')).slice(0,1).map(([p])=>p)];generated=false;lastBuild=null;modified=false;$('workspace-name').textContent=name;$('preview-title').textContent=name;$('file-search').value='';studio?.restore(workspace.studio??{});renderOptions(workspace.project??'',workspace.entry??'');renderEditor();renderFiles();save();if(restore)toast('Restored your local workspace.');compile();}
+function setWorkspace(workspace,{restore=false}={}){if(!workspace.files||typeof workspace.files!=='object')throw new Error('Workspace must contain a files map');const entries=Object.entries(workspace.files);if(entries.length>2000||entries.some(([p,t])=>typeof t!=='string'||t.length>4_000_000))throw new Error('Workspace exceeds safe interactive source limits');if(entries.reduce((n,[p,t])=>n+t.length,0)>16_000_000)throw new Error('Workspace exceeds 16 MB');snapshotFiles(workspace.files);workbench?.reset();recentlyClosed.length=0;collapsedFolders.clear();profiles.set(workspace.profile??{});development.set(workspace.development??{});files={...workspace.files};journal.reset(files);symbolSources={};name=workspace.title??workspace.name??workspace.id??'Local workspace';active=workspace.active&&files[workspace.active]!==undefined?workspace.active:entries.find(([p])=>/\.a?xaml$/i.test(p))?.[0]??entries[0]?.[0]??'';opened=workspace.opened??[active,...entries.filter(([p])=>p.endsWith('.cs')).slice(0,1).map(([p])=>p)];generated=false;lastBuild=null;modified=false;$('workspace-name').textContent=name;$('preview-title').textContent=name;$('file-search').value='';studio?.restore(workspace.studio??{});renderOptions(workspace.project??'',workspace.entry??'');renderEditor();renderFiles();save();if(restore)toast('Restored your local workspace.');compile();}
 function showDiagnostics(items=[]){queueMicrotask(()=>studio?.diagnostics(items));$('problems').replaceChildren();$('problem-count').textContent=items.length;if(!items.length){const empty=document.createElement('div');empty.className='diagnostic-empty';empty.textContent='✓ No problems in the supported compatibility profile.';$('problems').append(empty);return;}for(const d of items){const b=document.createElement('button');b.className='diagnostic-row';const i=document.createElement('span');i.className=d.severity==='warning'?'diag-warning':'diag-error';i.textContent=d.severity==='warning'?'△':'×';const code=document.createElement('span');code.textContent=d.code;const text=document.createElement('span');text.className='message';text.textContent=d.message;const where=document.createElement('span');where.className='location';where.textContent=`${d.file?.split('/').at(-1)??''}:${d.line??1}`;b.append(i,code,text,where);b.onclick=()=>{if(!files[d.file])return;openFile(d.file);const offset=d.offset??editor.value.split('\n').slice(0,(d.line??1)-1).join('\n').length;editor.focus();editor.setSelectionRange(offset,offset+(d.length??1));editor.scrollTop=Math.max(0,((d.line??1)-4)*23);syncScroll();cursor();};$('problems').append(b);}}
@@ -86,3 +90,3 @@ async function importFiles(fileList){try{const list=[...fileList];if(!list.lengt
 function selectLocal(){let item=$('samples').querySelector('[value="local"]');if(!item){item=document.createElement('option');item.value='local';$('samples').append(item);}item.textContent=name+' · local';$('samples').value='local';}
-editor.addEventListener('input',()=>{if(generated||!Object.hasOwn(files,active))return;files[active]=editor.value;lastBuild=null;profiles.invalidate();modified=true;$('saved').textContent='Saving…';updateHighlight();cursor();saveSoon();if($('live').checked){clearTimeout(timer);timer=setTimeout(compile,600);}});editor.addEventListener('scroll',syncScroll);for(const e of ['click','keyup','select'])editor.addEventListener(e,cursor);editor.addEventListener('keydown',e=>{if(e.key==='Tab'){e.preventDefault();const a=editor.selectionStart,b=editor.selectionEnd;editor.setRangeText('    ',a,b,'end');editor.dispatchEvent(new Event('input'));}});
+editor.addEventListener('input',event=>{if(generated||!Object.hasOwn(files,active))return;const previous=files[active];files[active]=editor.value;try{journal.observe(files,'Edit '+active,{group:event.inputType??''});}catch(error){files[active]=previous;editor.value=previous;updateHighlight();toast(error.message);return;}lastBuild=null;profiles.invalidate();modified=true;$('saved').textContent='Saving…';updateHighlight();cursor();saveSoon();if($('live').checked){clearTimeout(timer);timer=setTimeout(compile,600);}});editor.addEventListener('scroll',syncScroll);for(const e of ['click','keyup','select'])editor.addEventListener(e,cursor);editor.addEventListener('keydown',e=>{if(e.key==='Tab'){e.preventDefault();const a=editor.selectionStart,b=editor.selectionEnd;editor.setRangeText('    ',a,b,'end');editor.dispatchEvent(new Event('input'));}});
 document.addEventListener('keydown',e=>{if(e.defaultPrevented)return;if((e.ctrlKey||e.metaKey)&&e.key==='Enter'||e.key==='F5'){e.preventDefault();compile();}if((e.ctrlKey||e.metaKey)&&e.key==='s'){e.preventDefault();save();toast('Workspace saved in this browser.');}});
@@ -96,3 +100,3 @@ $('viewport').onchange=()=>{$('preview').style.width=$('viewport').value;};$('ex
 $('theme').onclick=()=>{document.body.classList.toggle('light');try{localStorage.setItem('jailbreak.theme',document.body.classList.contains('light')?'light':'dark');}catch{}};
-function createDocument(path){if(!path)throw new Error('Enter a source file path');if(!/^[\w .\-/]+\.(cs|a?xaml|csproj|props|targets|slnx)$/.test(path)||path.includes('..')||path.startsWith('/')){throw new Error('Use a relative C#, XAML, project, .props, .targets or .slnx path.');}if(Object.hasOwn(files,path)){openFile(path);return;}lastBuild=null;profiles.invalidate();files[path]=path.endsWith('.cs')?'namespace MyApp;\npublic class NewClass\n{\n}\n':/\.(csproj|props|targets)$/.test(path)?'<Project/>\n':path.endsWith('.slnx')?'<Solution/>\n':'<UserControl xmlns="https://github.com/avaloniaui">\n  <TextBlock Text="Hello"/>\n</UserControl>\n';renderOptions();openFile(path);modified=true;}
+function createDocument(path){if(!path)throw new Error('Enter a source file path');if(!/^[\w .\-/]+\.(cs|a?xaml|csproj|props|targets|slnx)$/.test(path)||path.includes('..')||path.startsWith('/')){throw new Error('Use a relative C#, XAML, project, .props, .targets or .slnx path.');}if(Object.hasOwn(files,path)){openFile(path);return;}lastBuild=null;profiles.invalidate();files[path]=path.endsWith('.cs')?'namespace MyApp;\npublic class NewClass\n{\n}\n':/\.(csproj|props|targets)$/.test(path)?'<Project/>\n':path.endsWith('.slnx')?'<Solution/>\n':'<UserControl xmlns="https://github.com/avaloniaui">\n  <TextBlock Text="Hello"/>\n</UserControl>\n';journal.observe(files,'Create '+path);renderOptions();openFile(path);modified=true;}
 for(const button of document.querySelectorAll('.activity [data-panel]'))button.onclick=()=>{for(const b of document.querySelectorAll('.activity button'))b.classList.toggle('active',b===button);for(const p of document.querySelectorAll('.side-panel'))p.hidden=p.id!==button.dataset.panel;if(innerWidth<=800)document.querySelector('.sidebar').classList.toggle('mobile-open');};
@@ -105,3 +109,22 @@ workbench=createWorkbenchShell({documents:()=>({...files,...symbolSources,...(la
  generated:()=>{generated=true;renderEditor();},notify:toast});
-studio=createStudio({development,active:()=>generated?'jailbreak-app.js':active,documents:()=>({...files,...symbolSources,...(lastBuild?.success?{'jailbreak-app.js':lastBuild.code}:{})}),open:(path,offset=0)=>{if(path==='jailbreak-app.js'){generated=true;renderEditor();}else openFile(path);editor.setSelectionRange(offset,offset);editor.scrollTop=Math.max(0,(editor.value.slice(0,offset).split('\n').length-4)*parseFloat(getComputedStyle(editor).lineHeight));syncScroll();cursor();},closeDocument,reopenDocument,createDocument,shell:workbench,compile,persist:save,notify:toast});
+function refreshWorkspace(path=active){
+  lastBuild=null;profiles.invalidate();modified=true;const project=$('project').value,entry=$('entry').value;renderOptions(project,entry);
+  opened=opened.filter(p=>Object.hasOwn(files,p));symbolSources={};generated=false;
+  path=Object.hasOwn(files,path)?path:Object.hasOwn(files,active)?active:Object.keys(files)[0]??'';
+  if(path)openFile(path);else{active='';renderEditor();renderFiles();}save();
+}
+const workspaceApi={journal,files:()=>files,
+  validate(candidate){
+    const options={...profiles.options(),...development.compileOptions(),projectPath:$('project').value||undefined,entryXaml:$('entry').value||lastBuild?.manifest.entryXaml||undefined};
+    return new Promise((resolve,reject)=>{const url=globalThis.__JailbreakAssets?.worker?URL.createObjectURL(new Blob([globalThis.__JailbreakAssets.worker],{type:'text/javascript'})):null;
+      const validator=url?new Worker(url):new Worker(new URL('./worker.js',import.meta.url),{type:'module'});
+      const cleanup=()=>{clearTimeout(timeout);validator.terminate();if(url)URL.revokeObjectURL(url);};
+      const timeout=setTimeout(()=>{cleanup();reject(new Error('Candidate validation exceeded 15 seconds'));},15000);
+      validator.onmessage=({data})=>{cleanup();resolve(data.result);};validator.onerror=event=>{cleanup();reject(new Error(event.message));};validator.postMessage({id:1,files:candidate,options});
+    });
+  },
+  apply(transaction){if(!development.canEdit())throw new Error('Finish or cancel execution/reload before applying source transactions');const entry=lastBuild?.xaml?.find(d=>d.className===lastBuild.manifest.entryXaml||d.path===lastBuild.manifest.entryXaml)?.path;const tx=journal.commit(files,transaction);refreshWorkspace(transaction.focus??active);if(entry&&Object.hasOwn(files,entry)){$('entry').value=entry;save();}return tx;},
+  history(direction){if(!development.canEdit())throw new Error('Finish or cancel execution/reload before undo');const tx=journal.apply(files,direction);if(tx){refreshWorkspace(Object.hasOwn(files,active)?active:historyFile(tx));if(development.options().enabled&&development.options().hotReload)compile();}return tx;}
+};
+studio=createStudio({workspace:workspaceApi,development,active:()=>generated?'jailbreak-app.js':active,documents:()=>({...files,...symbolSources,...(lastBuild?.success?{'jailbreak-app.js':lastBuild.code}:{})}),open:(path,offset=0)=>{if(path==='jailbreak-app.js'){generated=true;renderEditor();}else openFile(path);editor.setSelectionRange(offset,offset);editor.scrollTop=Math.max(0,(editor.value.slice(0,offset).split('\n').length-4)*parseFloat(getComputedStyle(editor).lineHeight));syncScroll();cursor();},closeDocument,reopenDocument,createDocument,shell:workbench,compile,persist:save,notify:toast});
 start();
diff --git a/apps/ide/style.css b/apps/ide/style.css
index 26915c8c895a0dcdc0e615b08ad2379ad554bba2..4872b548d3c82fb0bcc3a2ac6ec2f12980b31c1d 100644
--- a/apps/ide/style.css
+++ b/apps/ide/style.css
@@ -163 +163,13 @@ body.wb-studio{--wb-explorer:230px;--wb-inspector:330px;--wb-bottom:160px;--wb-s
 #grid-designer #grid-message{font-size:10px;margin:8px 0;line-height:1.6}.grid-track-card{padding:8px 0;border-top:1px solid var(--border)}.grid-track-spec{display:grid;grid-template-columns:42px minmax(0,1fr) auto;gap:5px;align-items:center}.grid-track-spec input{width:100%}.grid-track-chips{display:flex;gap:3px;overflow:auto;padding:7px 0}.grid-track-chips button{white-space:nowrap;color:var(--muted)}.grid-track-actions{display:flex;gap:5px;align-items:center;margin:5px 0;flex-wrap:wrap;font-size:10px}.grid-track-actions>input{width:46px}.grid-track-actions label{flex-direction:column;align-items:flex-start!important;gap:3px}.grid-track-actions label input{width:65px}.grid-cell-fields{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:5px;padding:8px 0}.grid-cell-fields label{display:block!important;font-size:9px}.grid-cell-fields input{display:block;width:100%;margin-top:5px}.grid-cell-map{display:grid;gap:3px;margin:9px 0;max-height:190px;overflow:auto}.grid-cell-map button{min-height:27px;font:10px ui-monospace,monospace!important}.grid-cell-map [aria-pressed=true]{color:var(--text);background:var(--hover)!important;outline:1px solid var(--accent)}
+/* Source change review is a local, accessible tool window, not a simulated Git client. */
+#workspace-review,#component-extract{color:var(--text);background:var(--panel);border:1px solid var(--line);border-radius:9px;box-shadow:0 24px 80px #0008;padding:0;max-width:calc(100vw - 24px)}
+#workspace-review{width:min(1100px,94vw);height:min(760px,88dvh)}#workspace-review[open]{display:flex;flex-direction:column}#workspace-review::backdrop,#component-extract::backdrop{background:#080b1370;backdrop-filter:blur(2px)}
+#workspace-review header,#workspace-review footer{display:flex;align-items:center;padding:12px 16px;gap:8px;background:var(--panel2);border-bottom:1px solid var(--border)}#workspace-review footer{border-top:1px solid var(--border);border-bottom:0;flex-wrap:wrap}#workspace-review h2{font-size:15px;margin:0 auto 0 0}#workspace-review p{font-size:11px;line-height:1.6;color:var(--muted);margin:10px 16px}#workspace-review-summary{font-size:10px;margin-right:auto;color:var(--muted)}#workspace-review button,#component-extract button{padding:6px 9px;font-size:11px;border:1px solid var(--border);border-radius:4px}#workspace-review #workspace-apply,#component-preview{background:var(--accent);color:var(--bg);font-weight:600}
+.workspace-review-toolbar{display:flex;gap:6px;padding:8px 16px;align-items:center;flex-wrap:wrap}.workspace-review-toolbar input,.workspace-review-toolbar select,#component-name{background:var(--editor);color:var(--text);border:1px solid var(--border);border-radius:4px;padding:6px;font-size:11px;min-width:0}.workspace-review-toolbar input{width:125px}.workspace-review-toolbar select{max-width:250px}
+.workspace-review-layout{display:grid;grid-template-columns:235px minmax(0,1fr);min-height:0;flex:1;border-top:1px solid var(--border)}#workspace-change-list{border-right:1px solid var(--border);overflow:auto;padding:8px;display:flex;flex-direction:column;gap:4px}#workspace-change-list button{text-align:left;overflow-wrap:anywhere;display:flex;align-items:center;gap:8px;flex-shrink:0}#workspace-change-list [aria-current=true]{background:var(--hover);border-color:var(--accent)}.change-mark{font:10px ui-monospace,monospace;color:var(--accent);flex-shrink:0}
+#workspace-diff{overflow:auto;background:var(--editor)}.workspace-diff-heading{position:sticky;top:0;background:var(--panel);display:flex;justify-content:space-between;gap:8px;align-items:center;padding:10px 13px;border-bottom:1px solid var(--border);font-size:11px}.workspace-diff-lines{font:11px/1.8 ui-monospace,monospace;padding:8px 0}.workspace-diff-row{display:grid;grid-template-columns:40px 40px 20px minmax(0,1fr);min-height:20px}.workspace-diff-row code{white-space:pre-wrap;overflow-wrap:anywhere;padding:0 8px}.diff-line-number{text-align:right;padding-right:9px;color:var(--muted);user-select:none}.workspace-diff-row.add{background:#25a66e20}.workspace-diff-row.remove{background:#e7526b20}.workspace-diff-row.gap{color:var(--muted);padding:6px;background:var(--panel2)}.workspace-diff-row.gap code{grid-column:1/5}
+#component-extract{width:520px;padding:20px}#component-extract h2{font-size:16px;margin-top:0}#component-extract p,.workspace-note{font-size:12px;line-height:1.6;color:var(--muted)}#component-name{width:100%;margin:10px 0}.workspace-note{margin-bottom:14px;font-size:11px}#component-cancel{float:right}#studio-extract-component{margin:10px 0;width:100%}
+@media(max-width:700px){.workspace-review-layout{grid-template-columns:1fr}#workspace-change-list{max-height:110px;border-right:0;border-bottom:1px solid var(--border)}#workspace-review{height:94dvh}#workspace-review header,#workspace-review footer{padding:8px}#workspace-review .workspace-review-toolbar{padding:8px}#studio-change-review{font-size:10px}.workspace-diff-row{grid-template-columns:28px 28px 16px minmax(0,1fr)}}
+
+/* Keep added workspace commands keyboard-reachable without widening mobile pages. */
+.vs-studio .studio-toolbar{min-width:0;max-width:100%;overflow-x:auto;scrollbar-width:thin}.vs-studio .studio-toolbar>button,.vs-studio .studio-toolbar>.studio-configuration,.vs-studio .studio-toolbar>.studio-debug-actions,.vs-studio .studio-toolbar>.studio-perspectives{flex-shrink:0}
diff --git a/packages/development/workbench.js b/packages/development/workbench.js
index b1a0032c460676c8ef316170479071662337c193..2c24199bf97fd5aef8064814902798d97883b809 100644
--- a/packages/development/workbench.js
+++ b/packages/development/workbench.js
@@ -6,3 +6,3 @@ import {mappedScript,updateGeneratedLocations} from './source-map.js';
 /** IDE client. The preview owns runtime objects; this client only sees serializable records. */
-export function createDevelopmentWorkbench({document:doc=globalThis.document,getFiles,getActive,openFile,applySource,build,persist,send,showGenerated=()=>{}}){
+export function createDevelopmentWorkbench({document:doc=globalThis.document,getFiles,getActive,openFile,applySource,build,persist,send,showGenerated=()=>{},sharedHistory=null}){
   const listeners=new Set();
@@ -54,3 +54,3 @@ export function createDevelopmentWorkbench({document:doc=globalThis.document,get
   let designSelection=[];
-  let settings={enabled:false,hotReload:false,cooperativeDebug:false,nativeBreaks:true,breakOnThrow:false,breakpoints:[],watches:[]},history=new EditHistory(),selected=null,running=null,runningFiles=null,pending=null,revision=0,selecting=false;
+  let settings={enabled:false,hotReload:false,cooperativeDebug:false,nativeBreaks:true,breakOnThrow:false,breakpoints:[],watches:[]},history=sharedHistory??new EditHistory(),selected=null,running=null,runningFiles=null,pending=null,revision=0,selecting=false;
   const tell=text=>status.textContent=text;
@@ -88,2 +88,3 @@ export function createDevelopmentWorkbench({document:doc=globalThis.document,get
   const api={button,panel,
+    canEdit(){return !pending&&!pausedTasks.size;},
     editSource(make){return sourceEdit(make);},
@@ -119,3 +120,3 @@ export function createDevelopmentWorkbench({document:doc=globalThis.document,get
     compileOptions(){return {debug:settings.enabled,cooperativeDebug:settings.enabled&&settings.cooperativeDebug};},
-    set(value={}){settings={enabled:!!value.enabled,cooperativeDebug:!!value.cooperativeDebug,hotReload:!!value.hotReload,nativeBreaks:value.nativeBreaks!==false,breakOnThrow:!!value.breakOnThrow,breakpoints:Array.isArray(value.breakpoints)?value.breakpoints.slice(0,1000):[],watches:Array.isArray(value.watches)?value.watches.slice(0,50):[]};enabled.checked=settings.enabled;cooperative.checked=settings.cooperativeDebug;hot.checked=settings.hotReload;native.checked=settings.nativeBreaks;throwBreak.checked=settings.breakOnThrow;watches.value=settings.watches.join(', ');history=new EditHistory();running=null;runningFiles=null;pending=null;revision=0;selected=null;tree.replaceChildren();propertyList.replaceChildren();pausedTasks.clear();pausedTask=pausedFrame=null;debugButtons.forEach(b=>b.disabled=true);renderBreakpoints();designSelection=[];publish('workspace-reset');},
+    set(value={}){settings={enabled:!!value.enabled,cooperativeDebug:!!value.cooperativeDebug,hotReload:!!value.hotReload,nativeBreaks:value.nativeBreaks!==false,breakOnThrow:!!value.breakOnThrow,breakpoints:Array.isArray(value.breakpoints)?value.breakpoints.slice(0,1000):[],watches:Array.isArray(value.watches)?value.watches.slice(0,50):[]};enabled.checked=settings.enabled;cooperative.checked=settings.cooperativeDebug;hot.checked=settings.hotReload;native.checked=settings.nativeBreaks;throwBreak.checked=settings.breakOnThrow;watches.value=settings.watches.join(', ');history=sharedHistory??new EditHistory();running=null;runningFiles=null;pending=null;revision=0;selected=null;tree.replaceChildren();propertyList.replaceChildren();pausedTasks.clear();pausedTask=pausedFrame=null;debugButtons.forEach(b=>b.disabled=true);renderBreakpoints();designSelection=[];publish('workspace-reset');},
     running(next,sources){pausedTasks.clear();pausedTask=pausedFrame=null;pauseState.textContent='No suspended invocation';debugButtons.forEach(b=>b.disabled=true);pending=null;revision=0;accept(next,sources);tell(settings.enabled?'Development session running.':'Release preview running.');publish('session-starting');},
@@ -133,3 +134,3 @@ export function createDevelopmentWorkbench({document:doc=globalThis.document,get
       else if(event==='selection-changed'){
-        designSelection=payload.items??[];for(const button of tree.querySelectorAll('[data-visual-id]'))button.setAttribute('aria-selected',String(designSelection.some(i=>i.id===button.dataset.visualId)));
+        designSelection=payload.items??[];if(selected&&!designSelection.some(item=>item.id===selected.id)){selected=null;selection.textContent='Select a visual to inspect its source.';propertyList.replaceChildren();runtimeProperties.textContent='';}for(const button of tree.querySelectorAll('[data-visual-id]'))button.setAttribute('aria-selected',String(designSelection.some(i=>i.id===button.dataset.visualId)));
       }
